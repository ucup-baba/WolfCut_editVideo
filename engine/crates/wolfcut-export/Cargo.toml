[package]
name = "wolfcut-export"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Rendering a timeline to a file, and one true frame of it to a monitor. The engine's ownership of what the pixels mean."

[features]
# The GPU compositor for full exports. Off by default for the same reason as
# wolfcut-render's: engine-only work never pays for the wgpu tree.
gpu = ["wolfcut-render/gpu"]
# TypeScript bindings for the wire types the UI sends (rasterised title
# overlays ride the export request as ExportClips). Off by default; see
# desktop/src/lib/generated/README.md for the generator command.
#
# Pulls wolfcut-project's bindings in with it: ExportClip carries that
# crate's `BlendMode`, which has to be a `TS` type for ExportClip to be one.
types = ["dep:ts-rs", "wolfcut-project/types"]

[dependencies]
wolfcut-core.workspace = true
wolfcut-media.workspace = true
wolfcut-render.workspace = true
wolfcut-project = { path = "../wolfcut-project" }
serde = { version = "1", features = ["derive"] }
ts-rs = { version = "12", optional = true }

[lints]
workspace = true
